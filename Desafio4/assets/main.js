function sumarProductos (precioUnitario, cantidadDeseada){
    return totalGastado = precioUnitario * cantidadDeseada;
}

let precio = 5500;
let cantidad = parseInt(prompt(`Ingrese la cantidad que quiera comprar: `));
sumarProductos(precio, cantidad);
alert(sumarProductos(precio, cantidad))